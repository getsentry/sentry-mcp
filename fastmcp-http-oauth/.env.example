# Server Configuration
PORT=3000
HOST=0.0.0.0
BASE_URL=http://localhost:3000

# Sentry (Self-Hosted) OAuth Application
# Create an OAuth app at: https://your-sentry.example.com/settings/account/api/applications/
SENTRY_HOST=sentry.example.com
SENTRY_CLIENT_ID=your-oauth-client-id
SENTRY_CLIENT_SECRET=your-oauth-client-secret

# Sentry OAuth Scopes (comma-separated)
# Default: org:read,project:read,project:write,team:read,team:write,event:write
# SENTRY_SCOPES=org:read,project:read,project:write,team:read,team:write,event:write

# Redis/Valkey Connection
# For local development: redis://localhost:6379
# For production with auth: redis://user:password@host:port/db
# For TLS: rediss://host:6379 (note the double 's' in rediss://)
REDIS_URL=redis://localhost:6379

# Redis/Valkey TLS Configuration
# Set to true when using encrypted connections (rediss://)
REDIS_TLS=false
# Set to false only for self-signed certificates (not recommended for production)
REDIS_TLS_REJECT_UNAUTHORIZED=true

# Security Keys (generate with: openssl rand -base64 32)
ENCRYPTION_KEY=your-32-char-encryption-key-here
JWT_SIGNING_KEY=your-32-char-jwt-signing-key-here

# OAuth Redirect URI Patterns (comma-separated, supports wildcards)
# IMPORTANT: Restrict this in production to your known client callback URLs
# Examples: "http://localhost:*", "https://*.example.com/callback"
# Default: "*" (allows any - NOT recommended for production)
# ALLOWED_REDIRECT_URI_PATTERNS=https://myapp.example.com/callback,https://localhost:*

# AI-Powered Tools (Optional)
# Required for: search_events, search_issues, search_issue_events, use_sentry
# These tools use embedded AI agents for natural language query translation
# OPENAI_API_KEY=sk-your-openai-api-key
# OPENAI_BASE_URL=https://api.openai.com/v1

# Documentation Tools (Optional)
# URL for the MCP server that serves documentation search/retrieval
# Default: https://mcp.sentry.dev (for search_docs and get_doc tools)
# MCP_URL=https://mcp.sentry.dev

# Logging
# Set to "debug" to see verbose FastMCP logs and filtered warnings
# LOG_LEVEL=debug
